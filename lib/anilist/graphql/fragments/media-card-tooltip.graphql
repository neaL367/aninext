fragment MediaCardTooltipFields on Media {
  isAdult
  bannerImage
  description(asHtml: false)
  studios(isMain: true) {
    nodes {
      id
      name
    }
  }
  tags {
    id
    name
    rank
  }
}
